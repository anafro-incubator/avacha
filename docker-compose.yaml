services:
    web:
        image: nginx:1.31-alpine3.23
        ports:
            - "80:80"
        volumes:
            - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
            - ./:/var/www/html

    app:
        build: .
        volumes:
            - ./:/var/www/html/
