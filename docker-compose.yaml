services:
    web:
        image: nginx:latest
        ports:
            - "80:80"
        volumes:
            - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf
            - ./:/var/www/html

    app:
        build: .
        volumes:
            - ./:/var/www/html/
